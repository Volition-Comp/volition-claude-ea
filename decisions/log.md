# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-06-10] DECISION: Connect Claude to Odoo using the lightweight local MCP connector `odoo-mcp` (tuanle96), run via `uvx`, with Odoo 19 JSON-2 transport. | REASONING: Odoo.sh + Odoo 19 + admin access + read/write desired. Machine had no Docker/Python/Node, so the Docker-based `odoo-claude-mcp` was overkill. `odoo-mcp` needs no Odoo-side module, supports Odoo 19 JSON-2, and runs locally for single-user Claude Code use. | CONTEXT: claude-integrations project, Phase A. uv is the only new tool installed. Graduate to an in-Odoo module + OAuth (Option B) later if team-wide audited access is needed.

[2026-06-11] DECISION: Add a second, separate Odoo MCP connector `odoo-prod` (pointed at production) alongside the existing staging `odoo` connector, rather than repointing the single connector back and forth. | REASONING: Keeps staging available for AI-agent dev while giving production its own clearly-labeled tool namespace (`mcp__odoo-prod__*`), reducing the risk of writing to the wrong instance. Prod uses its own API key in a separate env var (`ODOO_PROD_API_KEY`); writes enabled (`ENABLE_WRITES=1`). | CONTEXT: Triggered by needing to create a real part in production. Config edit to `.mcp.json` done with Adan's explicit approval (auto-mode initially blocked it as self-modification). Requires Adan to generate a prod API key, set `ODOO_PROD_API_KEY` via setx, and restart VS Code before the connector is live.

[2026-06-15] DECISION: Run Adan's daily "Morning Sync" as a hybrid: a scheduled cloud routine (8:00 AM MT, cron `0 14 * * *` UTC) drafts the Calendar + Inbox-highlights sections to a Gmail draft, while the Odoo sections (activity digest + open quotes) are pulled interactively in a local Claude session. | REASONING: Scheduled routines run in Anthropic's cloud and can only reach claude.ai OAuth connectors (Gmail, Calendar, Drive, Apollo, Upwork). Odoo is only a local stdio MCP, so the two most sales-relevant sections cannot run unattended yet. Hybrid starts the morning-sync habit now with what works and avoids a routine that silently half-fails. Built read-only (Gmail + Calendar connectors only; drafts, never sends). | CONTEXT: Reinforces and raises the priority of the remote/OAuth Odoo connector decision below. Once Odoo is a claude.ai connector, fold the Odoo digest + open-quotes sections into the same routine and drop the hybrid. Routine id `trig_01GdqTYFkSKTkt95B2L4qWNw`. Cron is fixed UTC and will drift to 7am when MST returns in Nov; bump to `0 15 * * *` then. A read-only DIGEST mode was added to the audit-activities skill for the interactive Odoo half.

[2026-06-15] DECISION: Pursue a remote/OAuth Odoo connector so the claude.ai app (not just Claude Code) can reach Odoo, enabling non-technical team members to use the Odoo-dependent skills. Validation-first: test the hosted Pantalytics "Odoo MCP Pro" free tier to confirm the team experience, then move to the in-Odoo module "Odoo MCP Studio" (`odoo_remote_mcp`, ~$261 one-time, OAuth 2.1+PKCE, installs on our Odoo.sh) as the permanent setup. | REASONING: The local `odoo-mcp` stdio connector is desktop-only (Claude Code / Desktop) and required heavy per-machine setup (uv, truststore TLS workaround, env-var API keys), so it can't serve the claude.ai app or scale to non-technical staff. claude.ai custom connectors require a remote MCP server with OAuth 2.1 + PKCE; both candidate products provide that and support Odoo 19 on Odoo.sh. Hosted-first lets us validate cheaply (free tier) before spending; the in-Odoo module keeps ERP data inside our own Odoo (no third-party data routing) and is a one-time cost vs. a subscription. | CONTEXT: claude-integrations project, "Option B" graduation noted in the 2026-06-10 entry. Skills with no Odoo dependency (humanize) and those needing only Gmail/Drive/Apollo are already app-ready and can go to the team independent of this work. Hosted test and module purchase/install both require Adan's account access and approval; not yet executed.

[2026-07-23] DECISION: Integrate Meyer Distributing via its official dealer API / data feed (wrapped in an MCP connector, same pattern as the Odoo connectors), not by scraping the login-gated dealer portal. First step queued: an access-request email to our Meyer rep Glenn Branham (Gmail draft). | REASONING: Quoting partitions for Mike Maroone Ford (S01439) required hand-entering Weather Guard + Holman parts as Misc lines because their cost/inventory only lives in `online.meyerdistributing.com`, which is password-gated and JS-rendered (WebFetch sees only the empty template). Meyer offers a dealer API (catalog, dealer-specific pricing, live inventory, UPC/MPN, vehicle fit) via API keys or FTP feed. An official-API MCP connector is robust and ToS-compliant; screen-scraping the authenticated site is brittle and likely prohibited. | CONTEXT: claude-integrations project, new "Meyer Distributing Connection" workstream (started 2026-07-23). Blocked on Adan sending the request and Meyer provisioning credentials + docs. Secret to live in a local env var (`MEYER_API_KEY`), never committed; expect the same TLS-inspection / truststore workaround as the Odoo connector. Could later auto-create Meyer parts in Odoo from the feed.


[2026-06-26] DECISION: Open a municipal end-user prospecting track alongside dealer calls. Built a Front Range public-fleet call list in Apollo (cities, counties, school districts), enriched ~99 fleet/transportation decision-makers, and stood up a Google Sheet lead tracker plus a call plan. | REASONING: Business slowed with the economy, so Adan and Tony are going out to drum up sales. Municipalities (school districts, cities, counties) run cargo/service-van fleets that need our upfits and buy in multi-vehicle batches, which feeds the 20-fleet-sales goal. End users were an untapped segment next to the usual dealership calls. | CONTEXT: fleet-sales-outreach project. Apollo people-search is free; enrichment cost ~99 lead credits (3,994 remaining). Direct-dial phone credits exhausted until July 7, so emails pulled now, phones later. Artifacts: projects/fleet-sales-outreach/municipal-fleet-call-list.md, municipal-fleet-leads.csv, municipal-call-plan.md, and Google Sheet "Front Range Municipal Fleet Leads — 2026-06". Sheet is structured for later import to Odoo as leads. Gaps: Adams 12 + a few districts lack a named transp director in Apollo.

[2026-07-17] DECISION: Restore the Stripe credit-card fee to the invoice PDF (which stopped printing after the V19 upgrade) by adding a new inherited QWeb view `volition_cc_fee.report_invoice_document_fees` on the standard `account.report_invoice_document`, and add a "Total Amt Processed" bold total below it. Deployed staging (view 7576) then prod (view 7577). | REASONING: The `fees` value (custom `stripe_fee_ext` module) is still computed and stored correctly on every `account.move`; only the PDF display broke. The upgrade repointed the invoice print action to Odoo's standard document template, which never referenced `o.fees`, and the old fee-printing customization did not survive. A thin inherited view that appends rows to the totals table is the least invasive, fully reversible fix (deactivate the view to undo). "Total Amt Processed" = amount paid (`amount_total - amount_residual`) + `o.fees` = what actually ran through the card; gated on `print_with_payments` so it only shows on the with-payments PDF. | CONTEXT: Both rows are `t-if="o.fees"` so cash/check invoices are unaffected. Anchor is `//table[hasclass('o_total_table')]`; Odoo validates the xpath at write time, and the create succeeding proves it matched. Verified visually by Adan on staging (INV/2026/00150) before prod deploy. Documented in references/odoo-known-issues.md. General upgrade hazard: report customizations that live outside a maintained module get reset when the upgrade regenerates report templates.
[2026-07-22] DECISION: Build GA4 ecommerce conversion tracking in-house instead of buying eb_website_sale_google_analytics_4 ($77, echoBitz). | REASONING: Stock Odoo 19 sends pageviews only, so a gap is real, but we already have GA4 and a GTM container live. The high-value piece (purchase event with order value and line items) is a single inherited QWeb view. Buying the module means another third-party dependency to carry through upgrades plus a Mediod deploy, for events we can maintain ourselves. | CONTEXT: Built and deployed to staging as view volition_ga4.confirmation_purchase_event (staging view 7577). Also found staging was firing prod's GA4, GTM, and Apollo tags because it's a prod copy, so staging clicks were polluting real reporting; tags stripped from staging same day. Prod deploy pending a DebugView test. See references/website-build-notes.md.
[2026-07-22] DECISION: Use Odoo 19's native GA4 ecommerce tracking; do NOT build or buy a custom purchase tracker. Deactivated the custom view volition_ga4.confirmation_purchase_event (staging view 7577). | REASONING: Corrects the same-day decision above. Odoo 19 ships full GA4 ecommerce tracking in core (website_sale/static/src/interactions/tracking.js): view_item, add_to_cart, checkout-step page views, and purchase, all via window.gtag. It's dormant until a GA4 measurement ID is set, which is why the site appeared to send nothing. Confirmed live on staging: view_item started landing in GA4 Realtime the moment the staging measurement ID was added. A hand-built purchase event on top of native would double-count every sale, so it was removed. | CONTEXT: Net effect for prod: conversion tracking needs nothing deployed beyond the measurement ID already present (G-977Z82K9WQ). Staging test order S01347 ($3,130, tx done) confirmed the confirmation page and native div[name=order_confirmation] (view 3688) are in place. Still to verify: a clean DebugView session showing the native purchase event end to end. See references/website-build-notes.md.
[2026-07-23] DECISION: Build and deploy custom GA4 checkout-funnel events (begin_checkout, add_shipping_info, add_payment_info) to make GA4's Purchase journey / Checkout journey funnels work. Deployed to prod (view volition_ga4.checkout_funnel_events, prod view 7581; staging 7578). | REASONING: Odoo 19 sends view_item/add_to_cart/purchase as real GA4 events but emits the mid-checkout steps only as virtual pageviews, so GA's built-in funnels read 0 from "Begin checkout" onward. Adding the three standard events fills the funnels, which show where online shoppers drop off (useful for Constance's 20-online-sales goal). Script is additive, wrapped in try/catch so it cannot affect a live checkout, and reversible (deactivate the view). | CONTEXT: Deployed on Adan's explicit approval without a live GA confirmation because every internal test device was blocked (McAfee WebAdvisor on normal Chrome + office HTTPS-inspection proxy) and GA's funnel/Events reports lag 24-48h, making same-day verification impossible. Real customers are unblocked, so verification is via the prod funnel reports in the days after 2026-07-23. Root-cause of the whole multi-session saga: McAfee WebAdvisor silently dropped GA collect requests on office machines; incognito (extensions off) let them through. See references/website-build-notes.md.

[2026-07-23] DECISION: Built a custom "Balance Sheet (Cash Basis, Adjusted)" report in Odoo STAGING (account.report id 29, variant of standard Balance Sheet root id 4) and left it for review before any prod port. Excludes AR, AP, Inventory Interim (both sides), Bills to Receive, and Tax Payable Cash Basis Transition; unfolds to account detail; QB-style equity that folds the cash-basis balancing figure into a computed Retained Earnings line (= Assets - Liabilities - Owner Equity - Net Income) alongside Net Income, with no visible "plug." | REASONING: Adan wanted a true cash-basis balance sheet that drops the accrual-only accounts distorting the standard cash-basis view, formatted like the QuickBooks statements Balanced Ledgers is used to. Building a report variant is a safe reporting view (changes no data). Odoo gotcha found: account-level unfold needs BOTH `groupby` and `user_groupby` = account_id on leaf lines, plus `horizontal_split_side` for the two-column layout. | CONTEXT: balance-sheet-cleanup project. Not ported to prod; two open decisions for Adan (exclude inventory? relabel?). Deliverables in projects/balance-sheet-cleanup/.

[2026-07-24] DECISION: Connect Claude to all four Google marketing platforms (GA4, Google Ads, Merchant Center, Search Console) as one unit: a single Google Cloud project with all four APIs enabled, one OAuth 2.0 Desktop client / shared refresh token, and one local read-only MCP connector `google-marketing` in `.mcp.json` (same stdio pattern as `odoo`/`odoo-prod`). Sequence: Search Console + GA4 first (fast, read-only, highest value), Google Ads developer-token request started in parallel (long pole), Merchant Center last. | REASONING: All four are Google APIs under one login, so they share auth and a connector instead of four separate setups. Triggered by hiring an SEO/paid specialist (Ryan Belcher) whose work depends on this data plus wanting to see rankings/traffic ourselves; also feeds Constance's online-sales goal. Read-only because we're consuming reporting, not managing campaigns. Local stdio (not a claude.ai remote connector) mirrors the Odoo choice: fastest for Adan's Claude Code use now, graduate later if the team needs it in the app. | CONTEXT: New project projects/google-marketing-integrations/ (README + phased checklist). GA4 site *tracking* already exists (measurement ID G-977Z82K9WQ); this is only the API read-back side. Google Ads needs a separately-approved developer token (Ads API Center, Basic access, ~days) — the one real hurdle, so start it first. Phase 0 (Cloud project, API enable, OAuth consent, Desktop client, env vars) is all Adan-logged-in console work; not yet executed. Expect the same TLS-inspection / truststore workaround as the Odoo connector.

[2026-07-24] DECISION: Built the `google-marketing` read-only MCP connector (local stdio, FastMCP) and added it to `.mcp.json`, exposing GA4 + Search Console tools now; Merchant + Ads deferred to when their creds land. | REASONING: Executes the same-day approach decision. GA4 + Search Console auth cleanly with the shared OAuth token (both verified live), so ship those as standing tools instead of one-off scripts. Merchant needs a Merchant ID and Ads needs the still-unapproved developer token, so their `@mcp.tool()` functions get added later; the token already carries their scopes. | CONTEXT: Files under `C:\Users\agonz\.config\google-marketing\` (server.py, client_secret.json, token.json, .venv), none committed; `.mcp.json` entry points the venv python at server.py with no secrets inline. Tools: search_console_sites, search_console_query, ga4_run_report. Editing `.mcp.json` done on Adan's explicit "formalize the connector" instruction. REQUIRES a VS Code restart to load, same as the Odoo connectors. TLS via truststore (HTTPS-inspection machine). See projects/google-marketing-integrations/README.md.

[2026-07-24] DECISION: Added Merchant Center to the `google-marketing` connector via Merchant API v1 (not Content API for Shopping, which is being sunset; v1beta was already discontinued Feb 2026). Registered GCP project 65201385905 with merchant account 5567027732 in-code via `accounts.developerRegistration:registerGcp` (one-time, no console step). Three tools added: merchant_account, merchant_product_status_summary, merchant_disapproved_products. | REASONING: Merchant API v1 is the current, supported path; the registration call is the documented fix for the "GCP project not registered" 401 and needs only an admin user (Adan). First pull is immediately useful: 664 products = 634 eligible / 27 disapproved / 3 pending. | CONTEXT: Google Ads remains the only unbuilt piece — blocked on a valid developer token (first paste returned DEVELOPER_TOKEN_INVALID). Also discovered the Ads gRPC client bypasses truststore and needs GRPC_DEFAULT_SSL_ROOTS_FILE_PATH -> a Windows CA PEM bundle (documented in the TLS notes). Requires a VS Code restart to load the new Merchant tools. See projects/google-marketing-integrations/README.md.

[2026-07-24] DECISION: Completed the `google-marketing` connector — all four platforms now live. Added `ads_campaign_performance` (Google Ads account 3451302235 via manager 5388014637). Full tool set: search_console_sites, search_console_query, ga4_run_report, merchant_account, merchant_product_status_summary, merchant_disapproved_products, ads_campaign_performance. | REASONING: The re-copied Ads developer token validated (first paste was garbled — real tokens are ~22 chars; must strip embedded whitespace from the pasted file). Ads returned real production data, so the token has Basic (not Test) access. gRPC TLS solved via a Windows CA PEM bundle. First Ads pull is a live finding: 4 enabled campaigns, ~$480/mo, with Sales-Shopping ($147, 526 clicks) and Refrigeration Packages ($122) at 0 conversions — half the budget returning nothing, and nobody managing it since Babita was let go. | CONTEXT: Requires a VS Code restart to load Merchant + Ads tools. All secrets local under C:\Users\agonz\.config\google-marketing\ (token.json, client_secret.json, ads_dev_token.txt, win_ca_bundle.pem), none committed. Next: audit the wasted ad spend and the 27 disapproved Merchant products; package Search Console baseline already drafted for the SEO hire. See projects/google-marketing-integrations/README.md.

[2026-07-24] DECISION: Add a GUARDED write layer to the `google-marketing` connector for Google Ads campaign management — `ads_set_campaign_status` (pause/enable) and `ads_set_campaign_budget`. Approval-first: each tool previews by default (confirm=False, no mutation) and only changes the live account when confirm=True AND `GOOGLE_ADS_ENABLE_WRITES=1`. Flow = I propose -> Adan approves -> I execute with confirm=True. | REASONING: Adan wants active campaign management (esp. to stop the ~$270/mo of 0-conversion spend), but campaign changes spend real money and are outward-facing, so they must not run unattended. Mirrors the Odoo guarded-write pattern (preview -> confirm). The Ads `adwords` OAuth scope is already full read/write, so no new Google authorization was needed; read-only until now was purely a tool-design choice. | CONTEXT: Built + tested on the preview/guard path only (verified: preview makes no change; confirm=True is refused while the switch is off). Turning the switch on (adding `GOOGLE_ADS_ENABLE_WRITES=1` to `.mcp.json`) was BLOCKED by the auto-mode classifier as a sensitive self-modification — left for Adan to add/approve, same as the 2026-06-11 Odoo prod write enablement. Not yet active. Requires a VS Code restart to load the write tools + env. Discovered Sales-Shopping runs a $5/day budget ($150/mo) at 0 conversions. See projects/google-marketing-integrations/README.md.

[2026-07-27] DECISION: Auto-populate `expected_revenue` on CRM opportunities from their linked quotes via a base_automation rule, and lock the field from manual editing once a quote exists. Built and verified on STAGING (rule 27, server action 1891, views 7580 form + 7581 list). Prod deploy and backfill deferred to Adan's go-ahead. | REASONING: Expected Revenue was hand-entered and drifted badly (prod opp 1165 read $46,078.04 against a $46,694.04 quote; opps 1157 and 1164 sat at $0 with live quotes). Odoo's built-in `sale_amount_total` can't drive it: not stored, and confirmed orders only. Adan chose confirmed-sum-else-latest-quote over summing everything, because summing double-counts revisions (opp 1164 has two live quotes for one job) while latest-only understates genuinely multi-order deals (opp 300, Bachelor Gulch, 8 orders). Untaxed, matching how he filled it in by hand. Always overwrite, so the quote is the single source of truth. Locking the field conditionally (read-only only once a quote is attached) keeps early-stage estimating possible on opps that have no quote yet. | CONTEXT: Verified on staging: opp 1134 tracked the newest of two drafts, not the edited one; opp 864 summed two confirmed orders correctly; all test edits reverted. Two gotchas worth remembering, both in references/odoo-known-issues.md: base_automation does NOT fire when you write a field its existing value (so no-op writes are useless as a manual trigger and read as a broken rule), and fields referenced in a view modifier must be injected into your own inherited view rather than borrowed from sale_crm's smart-button nodes, which silently produced a view that saved clean and did nothing. Known gap left in deliberately: reassigning a quote to a different opportunity updates the new opp but leaves the old one stale. `state` is readonly over MCP, so the confirm-a-quote path was verified by Adan in the staging UI.

[2026-07-28] DECISION: Deployed the Expected Revenue sync to PRODUCTION (base.automation 27, server action 1892, views 7582 form + 7583 list) and backfilled 14 selling-stage opportunities, net +$88,407. Added a zero-value-quote guard to the rule on both instances before backfilling. | REASONING: Staging verified clean, including Adan confirming a quote in the UI. The guard was added because the backfill dry run showed opp 1098 (CU Boulder Primary Care Van, a real $85,000 estimate) would be zeroed by an empty $0 draft quote attached to it. A blank quote shell has nothing to populate from, so it is now ignored the same way cancelled quotes are. This is a narrow exception to "always overwrite," consistent with the existing all-cancelled rule, not a reversal of it. | CONTEXT: Backfill scoped by STAGE, not by won/lost status, after finding 425 opportunities parked in a stage named "Archive" holding $3.29M of stale expected revenue that a `won_status = pending` filter would have rewritten. Post-sale stages (In Production, Invoiced, Delivered & Unpaid, Off-Site) left untouched at Adan's choice; only 10 of 34 differed and the net was -$171 of rounding. Opp 1157 "State Bid Quotes" deliberately skipped and left at $0: it is a bucket holding 25 unrelated live drafts (~$163k), where neither the newest quote ($51,841.05) nor the sum means anything. NOTE: the automation will set 1157 to the newest quote as soon as anyone edits one of those 25, so the real fix is splitting it into separate opportunities. See references/odoo-known-issues.md.

[2026-07-24] DECISION: Enabled the Ads write switch — added `GOOGLE_ADS_ENABLE_WRITES=1` to the `google-marketing` env in `.mcp.json`, with Adan's explicit in-session approval of the edit (auto-mode had blocked it). Guarded write tools are now armed (still approval-first: preview -> Adan okays -> execute with confirm=True). | REASONING: Adan chose to turn on active Ads management to address the wasted spend. | CONTEXT: Takes effect after a VS Code restart. First intended action: preview a pause of Sales-Shopping ($5/day, 0 conversions). See the guarded-write-layer entry above and projects/google-marketing-integrations/README.md.

[2026-07-23] DECISION: Hold the van-disposal-loss reclass (account 400104 "Gain/Loss on Disposal of Van", $12,255.90, mis-typed as off_balance so the April-2024 loss never hit the P&L) until Adan speaks with the CPA. Tested the fix (retype to expense) in STAGING only; production account unchanged. | REASONING: Correcting it moves a $12,256 loss into 2024, changing that year's net income and taxable income. If 2024 is closed/filed it needs a current-dated reclass, not a reopening. That's a CPA (Balanced Ledgers) call, not a bookkeeping one. | CONTEXT: Surfaced via the Off-Balance-Sheet section of the new report. One of several chart-of-accounts / reconciliation issues in the balance-sheet-cleanup project.

[2026-07-25] DECISION: Fix "can't see quotes on opportunities" by adding a read-only "Quotes & Orders" tab to the crm.lead form (lists `order_ids` regardless of state) rather than fighting Odoo's mobile layout. Built on STAGING as view 7579; prod deploy held for Constance's testing. | REASONING: Two separate causes, and a device-level workaround fixes neither for the team. (a) Odoo hides the entire smart-button row below 768 CSS px (`isSmall` <= SIZES.SM in web/static/src/core/ui/ui_service.js), a hard framework cutoff with no overflow menu. iPhone landscape widths straddle it exactly: SE/7/8 = 667 and 8 Plus = 736 (no buttons), iPhone X and newer = 812+ (buttons). That's why Adan saw nothing and Tony, same app and same record, saw everything. (b) `quotation_count` only counts draft/sent orders; on confirmation the value moves to `sale_order_count` and the Orders button, so every WON opportunity reads "Quotations 0" even with a confirmed sale attached. That one hits desktop users too. A tab in the record body renders at any width and shows quotes and confirmed orders together, so it solves both without anyone needing to know about a pixel threshold. | CONTEXT: Triggered by opportunity 1149 "Shelf Add-On For Stryker Van" (won, Quotations 0, but S01337 for $2,648.90 confirmed and correctly linked). Read-only on purpose so quotes are still created via the existing New Quotation button; hidden on leads. Test records on staging: 1135 (one confirmed order), 1136 (two drafts), 1140 (two sent). Additive and reversible, deactivating the view removes it and no data is touched. Also confirmed `quotation_count`/`sale_order_count` are non-stored and non-searchable, so a domain on them silently returns 0 rows. Full writeup in references/odoo-known-issues.md. Draft email to Constance requesting testing is in Gmail.

[2026-07-24] DECISION: Removed the broken canonical-tag block from the production website's Custom Code -> head field (`website` record 1), leaving Odoo's native canonical as the only one on the page. Left the GTM container (GTM-P5MW3RZ) and the Apollo tracker in that field untouched. | REASONING: Someone had pasted a QWeb template snippet into the Custom Code head field: `<t t-name="website.layout" t-inherit="website.layout" t-inherit-mode="primary">` wrapping `<link t-att-href="request.httprequest.url" rel="canonical"/>`. That field injects raw HTML and does not compile QWeb, so the inheritance wrapper did nothing and the inner tag shipped literally to the browser on every page with no `href` at all. Every page was serving two canonical tags, one of them empty. Google discards all rel=canonical hints when it sees more than one, so the site effectively had no canonical signal. That explains why www and non-www both rank as separate pages in Search Console (homepage split 58 / 57 clicks across the two hostnames) and matches SEO contractor Ryan Belcher's report that canonical tags had been "removed." Odoo already emits a correct native canonical, so the fix was deletion, not addition. | CONTEXT: Found while pulling a Search Console + page-level baseline for the SEO work. Guarded prod write (validate -> preview -> execute). Verified live on /, /shelving-westcan, and /about-us: exactly one canonical each with the correct URL, no leftover `t-att-href`, GTM and Apollo tracking both still firing. Pre-change value backed up to the session scratchpad as custom_code_head.BACKUP-2026-07-24.html; to roll back, restore that string to website.custom_code_head. Recovery of rankings depends on Google recrawling, so expect weeks, not days. Related issue found but NOT fixed: https://volitioncomponents.com redirects to **http**://www.volitioncomponents.com (drops to insecure http before bouncing to https), which should be a single 301 straight to https://www. Lesson for the future: Custom Code head/footer takes plain HTML and JS only; anything needing QWeb has to be a real `ir.ui.view`, per references/website-build-notes.md.

[2026-07-24] DECISION: Deployed the "Balance Sheet (Cash Basis, Adjusted)" report to PRODUCTION (account.report id 29, variant of the standard Balance Sheet root id 4). Rebuilt from the staging version after re-resolving the excluded account ids in prod (Inventory Interim Delivered 4 / Received 3, Bills to Receive 15, Tax Cash Basis Transition 159 — matched staging). QuickBooks-style format: nested "LIABILITIES AND EQUITY", Credit Cards split out, QB labels, equity as Owners' Equity / Retained Earnings / Net Income; unfolds to account level. | REASONING: Adan approved the prod deploy after reviewing the staging report and verifying format/accounts against Volition's 2023 QuickBooks balance sheet. A report is a read-only reporting view — deploying changes no ledger data. | CONTEXT: balance-sheet-cleanup project. The van-loss account retype was NOT deployed to prod (still on hold for the CPA), so the prod report's Off-Balance section still shows the $12,255.90 until that's addressed.

[2026-07-24] DECISION: Reclassified the van-disposal loss in PRODUCTION - changed account 400104 "Gain/Loss on Disposal of Van" from account_type off_balance to expense_other. | REASONING: The CPA confirmed the $12,255.90 loss was recognized in 2024. The source entry (MISC/2024/04/0005) is already dated 2024-04-24, so correcting the account type flows the loss onto the 2024 P&L and moves it out of the off-balance section into Retained Earnings on the current balance sheet. Previously on hold pending CPA. | CONTEXT: balance-sheet-cleanup project. Effect: 2024 net income reduced by $12,255.90. Same fix previously validated in staging. Reversible (account type can be changed back). Artifact + cleanup worksheet updated to mark it resolved.

[2026-08-03] DECISION: Move forward with Meyer Distributing's FTP pricing/inventory feed as the integration path, and split Meyer into its own project at projects/meyer-integration/. | REASONING: Zach Wood (Director of Ecommerce Sales) confirmed an FTP feed carrying dealer cost, MAP, core charge, Meyer + MFG part numbers, UPC, short description, DIMs, LTL/oversize flags, and inventory on hand. He said an API is also possible but gave no detail. Every feed field except vendor inventory-on-hand maps cleanly onto product.template / product.supplierinfo, so the feed alone delivers auto-updating cost plus a Meyer availability signal. The work has enough of its own surface area now to stop living as a section inside claude-integrations. | CONTEXT: Meyer explicitly does NOT provide images or vehicle fitment ("we do not own that data"), which contradicts our earlier research assumption. Fitment/content will need a separate source (SEMA Data Co-op PIES/ACES, or brand-direct) and is parked. Follow-up email to Zach drafted in Gmail asking for FTP specs, inventory granularity by DC, API capabilities (especially PO submission + order status/tracking), cost, and MAP/data-use terms. Contact chain: Glenn Branham never responded, Rocio Marquez routed us to Zach.

[2026-08-03] DECISION: On S01396 (Strobe Light Package, in the State Bid Quotes opp 1157), add the customer's strobe spec as a visible line note but deliberately omit the light class. | REASONING: The bid spec sheet (image in the SO chatter) calls for a Class 3 light, but Federal Signal's Brian Robertson emailed that Class 3 is not approved for on-road use, and the part actually quoted is the Class 2 Firebolt beacon. Naming a class on the customer-facing quote would contradict the part being sold, so the note carries the physical/install requirements (roof-mount, SAE certified equivalent, 75+ flashes/min, 12V, in-cab switch, 1-yr warranty, centered + 2 ft from rear, weatherproof, constant power) without a class number. | CONTEXT: The class conflict is unresolved and is Adan's to settle with the customer. First stored attempt silently dropped the final line; Odoo truncates line_note text on create, so the note was re-written and verified. Firebolt Specs.pdf is attached to S01396's chatter for reference.

[2026-08-03] DECISION: Set the quotation expiration (validity_date) to 2027-12-31 on all 24 quotes in the State Bid Quotes opportunity (opp 1157, Ken Garff Ford Greeley), replacing the prior 8/14-8/16/2026 dates. | REASONING: Adan's instruction. State solicitations commonly require a long price hold, so a single far-out date across the whole bid is intended. | CONTEXT: ~17-month hold. Exposure flagged to Adan: the six Westcan quotes carry a 22% import tariff line and the shelving is priced off current vendor cost, so any accepted late in that window is locked at today's numbers. Guarded write, verified by re-reading all 24. Editing these quotes did NOT trip the expected_revenue automation (no order's amount_total changed; opp 1157 still reads $0) - but per the 2026-07-28 note, any edit that changes a quote total will, so the real fix remains splitting this bucket opp into separate opportunities.

[2026-08-03] DECISION: Decode Ford Transit VINs with the Ford Pro Transit body decoder (VIN digits 5-7), not the NHTSA vPIC API. Written up in references/vin-decoding.md. | REASONING: On the Mike Maroone quote (VIN 1FTBR2C86TKA83440) I read NHTSA's WheelBaseFrom/WheelBaseTo (130/148) as the vehicle's wheelbase, but those fields return the range for the whole Transit line, not the value for that VIN. NHTSA also returned Trim "Medium Roof" plus Trim2 "EL", a combination Ford does not build (Extended Length is High Roof only on the 2026 Transit). Adan caught it and supplied the body decoder. Body code R2C = Transit 250, Medium Roof, 148" WB. | CONTEXT: Roof height and length pick the Westcan shelving package (ABP9660 = 96" + 60" for 148" WB MR/HR vs ABP6060 = three 60" for 148" WB EXT), so a bad decode mis-quotes the job. The wrong read would have put two 60" units on this quote instead of one 96", about $1,374 too high on the shelving section. vPIC is still fine for year/make/model/series/GVWR/plant.

[2026-08-05] DECISION: Deploy the CRM "Quotes & Orders" tab straight into prod (view 7584) instead of rebuilding it on staging first, and skip Constance's pre-test. | REASONING: Adan's call. The change is one inherited view on crm.lead.form (id 2389), read-only, touches no data, and reverts by archiving the single view, so a staging round trip bought nothing but delay. The original staging build (view 7579, 2026-07-25) had already been held for testing for two weeks and was then destroyed in the staging rebuild without ever shipping. Staging is also currently unreachable (host recycled, DNS failure), so waiting meant waiting on new credentials too. | CONTEXT: The tab fixes two compounding problems documented in references/odoo-known-issues.md: Odoo hides the whole smart-button row below 768px so quotes are invisible on smaller iPhones, and quotation_count drops to 0 the moment a quote is confirmed. Verified server-side via execute_method get_view on crm.lead (the page and all six sale.order fields appear in the combined arch) before asking Adan to look, then confirmed by Adan on mobile. Cost us one false alarm first: the Odoo mobile app caches view definitions and showed nothing until the app was fully quit and reopened. Lesson recorded in the same reference: verify with get_view before suspecting the view.

[2026-08-03] DECISION: Every quote with shelving or mounted accessories gets a fitment check against the van's interior dimensions before the preview goes to Adan, and the result is reported per side even when it's clean. Added as step 6 of the write-estimates skill with a six-point procedure; dimensions captured in references/van-interior-dimensions.md. | REASONING: Adan's instruction, prompted by real interference cases: a tank rack behind a shelf hitting the D-pillar, no room for a tank rack on the driver's side, end panels landing on a wheel well. Those get caught on the shop floor today, which costs a rework or a credit. The dimensions were never written down anywhere we could check against. | CONTEXT: Primary source is the Westcan 2026 catalogs (updated 2026-07-08), which zone every van A through L to the 1/16" and are downloadable as PDFs behind the FlowPaper viewer at westcanmanufacturing.com/catalogues. The Sortimo by Knapheide catalogs in Drive (Knapheide > Exxpand Archives) are kept as a secondary cross-check, but the two vendors place the partition differently so their numbers run 5" to 20" apart on the same van. Don't mix sources within one quote. Sharpest traps recorded: 148 Transit low/medium roof has only 6 1/16" of post-wheel-well zone, the Transit 130 medium roof is ~7" shorter than the low roof, and dual rear wheel Sprinters intrude 16" versus 8 1/2".

[2026-08-06] DECISION: Adopt two standing engineering roles (specialty upfit sales engineer, and industrial engineer for standard work), sequence design before SOPs, and amend the team rollout plan with three items it never considered. Plan artifact + projects/claude-integrations/team-rollout-plan.md. | REASONING: Adan and Tony are still the answer key for everything, and Tony spends his day directing techs. Adan put the split at 70% design defects / 30% execution gaps, which decides the order: better SOPs first would be effort on the smaller half, and would put polished paper in front of incomplete designs. Authority model is posture 2, authoritative with a named escalation list; large builds go to an outside engineer after the sale closes to conserve resources, small builds get proofed by Tony or the future foreman. | CONTEXT: Found that the Odoo Quality module is already installed with ~30 control points against real operations (Liner Kit, Reefer, Partition, 2K Inverter) plus a "Reefer Van Inspection" worksheet template, but every point is test type Instructions, the one mode that cannot record whether anyone read it, and no point has a worksheet attached. That is very likely why the checkout practice died and why Esme has nothing to sign. The stage gate we need is already paid for. Three amendments to the rollout plan: "Tony is app-only, he doesn't need the repo" no longer holds now that the goal is capturing his knowledge; Claude Code web/desktop was never considered and removes the uv/TLS/McAfee barrier that excluded him; and nothing in the plan carries decisions back up from app users to the repo.

[2026-08-06] DECISION: Sales orders get sequenced in order of operations, and the electrical section splits into rough-in (early) and wiring finals + commissioning (late). Applied to S01441. | REASONING: Adan's convention is that SO sections drive the work-order sequence, and I built S01441 in the order he walked me through the van instead. Prewire landed at section 110, after cabinetry at 20 and after insulation and walls at 60, so as written those work orders told a tech to hang cabinets before the wire was pulled. Prewire must precede insulation while finals must follow cabinetry and accessories, so one combined electrical section cannot sit in the right place. | CONTEXT: New order is prewire, generator, heater, A/C and roof penetrations, lining/insulation/floor, overhead lighting, inverter, cabinetry, refrigeration, sink, bed, awning, step, grab handles, seat swivels, scene lighting, finals. The finals section now ends with commissioning: verify on shore, on generator, on inverter, verify transfer, verify every GFCI, confirm both vaccine units hold temperature on each source. That is the first real QC gate on a Volition order.

[2026-08-06] DECISION: On S01441, delete the RP-9879-55C4 converter, the IB500 500A relay and the AGM start battery; keep one charger (the MultiPlus-II downstream of the transfer switch) and one alternator path (upsized to a Victron Orion XS 12/12-50A); swap the Square D QO panel for a Progressive Dynamics PD5000. | REASONING: I built the generator package off S00150/S00865 (a lead-acid van) and bolted the S00506 Victron lithium stack on top without reconciling them, which produced exactly the two failure modes Adan named: a lead-acid-profile charger redundant with the MultiPlus on a LiFePO4 bank, and a 500A isolator hard-paralleling alongside a DC-DC so the lithium could bypass the DC-DC current limit and back-feed the alternator. Adan confirmed Douglas removed the converter and AGM for the same reason. Orion-Tr at 18A could not hold the 22A worst-case refrigeration draw the design depends on between destinations. | CONTEXT: Also caught: Eaton BR breakers specced into a Square D QO panel (they do not fit), a 2-space panel for 6+ circuits, and a NEMA 5-20R receptacle with no 20A breaker or 12 AWG. The PD5000 fixes all of it and adds 12 fused DC branches, which the quote had none of. Note Greeley Bomb Squad S00926 carries the same PD panel plus Square D QO breakers, so that trailer may have the identical mismatch and is worth eyeballing. Standard to write: panel and breaker brand are specified together as a pair, never separately. Open for the post-sale engineer: generator cranking off the lithium bank (BMS surge rating), and neutral-ground bonding across shore/generator/inverter.
